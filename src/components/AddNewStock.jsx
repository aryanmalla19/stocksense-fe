import React from "react";
import Button from "./Button";

const AddNewStock = () => {
  return (
    <div>
      Add New Stock
      <Button classes="bg-teal-500 hover:bg-teal-700">Add</Button>
    </div>
  );
};

export default AddNewStock;
